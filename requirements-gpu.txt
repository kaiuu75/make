# GPU dependencies, additive on top of requirements.txt.
#
# For the AMD MI300X droplet (Ubuntu 24.04 + ROCm 6.x) install PyTorch from
# the ROCm wheel index — these wheels expose the familiar torch.cuda.* API
# but run on HIP/ROCm under the hood.
#
#   python3 -m pip install --upgrade pip
#   python3 -m pip install -r requirements.txt
#   python3 -m pip install --index-url https://download.pytorch.org/whl/rocm6.2 \
#       torch==2.5.1 torchvision==0.20.1
#   python3 -m pip install -r requirements-gpu.txt
#
# On NVIDIA hosts, swap the index for the matching CUDA build.

# Deep learning stack (installed above — listed here for reference only).
# torch>=2.4,<3.0
# torchvision>=0.19,<1.0

# Training utilities
einops~=0.8
rich~=13.9
tensorboard~=2.18

# Faster geospatial I/O under parallel preprocessing.
zarr~=2.18
numcodecs~=0.13

# GPU diagnostics (works on ROCm via rocm-smi wrapper; optional import).
pynvml; platform_system == "Linux" and platform_machine == "x86_64"

# Optional speed-ups — uncomment on ROCm if available as wheels.
# flash-attn
# xformers
